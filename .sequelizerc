const path = require('path');
const SERVER_ROOT = 'src/';

module.exports = {
  'config': path.resolve(SERVER_ROOT + 'config', 'config.json'),
  'models-path': path.resolve(SERVER_ROOT + 'models'),
  'seeders-path': path.resolve(SERVER_ROOT + 'seeders'),
  'migrations-path': path.resolve(SERVER_ROOT + 'migrations')
}